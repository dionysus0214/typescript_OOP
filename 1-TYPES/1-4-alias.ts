{
  // Type Alias
  type Text = string;
  const name: Text = 'joshua';
  type Student = {
    name: string;
    age: number;
  }
  const student: Student = {
    name: 'barbie',
    age: 15,
  }

  // String Literal Types
  type JSON = 'json';
  const json: JSON = 'json';
}